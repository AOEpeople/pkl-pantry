/// Module that provides a configurable golang fmt job
module com.gitlab.ci.golang.jobs.fmt

import ".../golang.pkl"

/// Golang fmt job
class Job extends golang.AbstractGolangJob {
  inputs = super.inputs |> golang.withArg("./...")
  script {
    "go fmt \(inputs.args.join(" "))"
  }
}
