amends ".../basePklProject.pkl"

package {
  version = "0.1.1"
  description = """
    Package that provides the schema for GitlabCI
    """
}

dependencies {
  ["aoestrings"] = import("../com.aoe.opensource.strings/PklProject")
}
