module com.gitlab.ci.schema.tests.pipeline
amends "pkl:test"

local allExamples = import*("../examples/pipeline/*.pkl")

examples {
  for (example in allExamples.keys) {
    [example.drop("../examples/pipeline/".length).replaceLast("pkl", "yml")] {
      allExamples[example].output.text
    }
  }
}
